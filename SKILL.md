---
name: project-modifier
description: "修改已有项目的全流程管理 skill。当用户需要对一个已有项目进行修改时，必须使用此 skill。适用场景包括：修复 bug、新增功能、删除功能、调整接口、重构代码、调试、补丁、热修复、调整、更新、升级、迁移等。该 skill 完全独立运行，不依赖其他 skill。核心机制：代码为主（以实际代码为真相源，文档仅作参考）、分层代码理解（渐进式理解项目，只聚焦修改相关区域）、规模分级（小/中/大三种修改规模对应不同重量的流程）、规划文档驱动（中大型修改必须先形成规划文档再执行）、三级回退（修复→回退本次修改→回退到接手前需用户确认）。当目标为已有项目时，此 skill 优先于项目构建类 skill。当用户说'帮我修个bug'、'给项目加个功能'、'删除这个模块'、'重构一下'、'改一下接口'、'调试'、'补丁'、'更新'、'升级'、'fix a bug'、'add a feature'、'refactor this'、'modify the project'、'debug'、'patch'、'hotfix'、'tweak'、'adjust'、'update'、'upgrade'、'remove'、'delete'、'migrate'等任何涉及修改已有代码的请求时，无论中英文，都应触发此 skill 而非项目构建类 skill。即使用户只是简单说'改一下xxx'，如果涉及对已有代码的修改，也应使用。"
metadata:
  author: Z.AI
  version: "1.0"
  tags: ["project-modification", "bug-fix", "feature-addition", "refactoring", "maintenance"]
---

# Project Modifier — 修改已有项目的全流程管理

## 核心理念

这个 skill 解决的核心问题是：如何安全、可控地修改一个你可能不完全了解的已有项目。以下是四个基石原则：

**1. 代码为主（Code is Truth）**

已有项目中，代码是唯一确定的真实。文档可能过时、可能缺失、可能自相矛盾，但代码是实际运行的。因此，所有理解和决策都基于代码，文档只做参考。修改后同步更新文档，但不主动全面重写文档。

**2. 渐进理解（Progressive Understanding）**

不要试图一次性理解整个项目。先宏观把握，再理解数据流，最后聚焦与修改相关的区域。理解的范围和深度随修改规模而定——改一个 bug 不需要理解整个架构。

**3. 规划先行（Plan Before Act）**

中大型修改必须先形成规划文档再动手。规划文档是修改全程的指引，执行严格按规划进行，不允许偏离。如果发现规划有问题，先改规划再继续。

**4. 安全修改（Safe Modification）**

每一次修改都要确保可回退、可验证、可追溯。修改前建立安全网（Git 保护 + 影响分析），修改后验证结果（测试 + 文档同步），修改全程留痕（CHANGELOG + Git 历史）。

---

## 全局流程总览

```
用户提出修改需求
     │
     ▼
┌──────────────────┐
│  M0 项目评估      │ ← 确认 Git/文档/测试/技术栈状态
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│  M1 代码理解      │ ← 分层渐进理解，聚焦修改区域
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│  M2 修改点分析    │ ← 明确要改什么 + 影响范围
│  + 规模判断       │ ← 判断：小型 / 中型 / 大型
└──────┬───────────┘
       │
       ├── 小型 ──────────────────→ 直接修改 → 测试 → 提交
       │
       └── 中型/大型
            │
            ▼
       ┌──────────────────┐
       │  M3 规划文档      │ ← 输出修改规划文档
       │                  │   如需则同步输出接口/测试文档
       │                  │   用户确认后方可执行
       └──────┬───────────┘
              │
              ▼
       ┌──────────────────┐
       │  M4 执行修改      │ ← 按规划文档实施
       │                  │   每步测试验证
       │                  │   代码与规划文档保持一致
       └──────┬───────────┘
              │
              ▼
       ┌──────────────────┐
       │  M5 验证与收尾    │ ← 全量测试 + 文档更新
       │                  │   CHANGELOG + Git 提交
       └──────────────────┘
```

---

## 各阶段详细说明

### M0 — 项目评估

**目标**：快速摸清项目的基本状态，建立修改的基础设施。

**执行步骤**：

1. **确认项目位置和类型**：找到项目根目录，识别技术栈
2. **确认 Git 状态**：
   - 有 Git → 检查当前分支、是否有未提交变更、最近的 commit 记录
   - 无 Git → 初始化 Git 仓库，先提交当前全部代码作为基线
3. **确认文档状态**：
   - 有文档 → 浏览文档，标记文档与代码可能不一致之处
   - 无文档 → 标记为无文档状态，后续理解代码时逐步补写
4. **确认测试状态**：
   - 有测试 → 运行一遍，记录当前测试通过率
   - 无测试 → 标记为无测试状态
5. **确认代码风格**：观察已有的代码风格（缩进、命名规范、注释风格等）

**输出产物**：项目状态快照（内部记录，不一定要输出文件）

| 项目 | 状态 |
|------|------|
| 技术栈 | |
| Git 状态 | 有/无，当前分支，未提交变更 |
| 文档状态 | 有/无/部分，可能过时的文档 |
| 测试状态 | 有/无，通过率 |
| 代码风格 | 简要描述 |

**Git 操作**：
- 无 Git 时：`git init && git add . && git commit -m "chore: initial commit before modification"`
- 有 Git 但有未提交变更：先提交 `git add . && git commit -m "chore: commit current state before modification"`

---

### M1 — 代码理解

**目标**：渐进式理解项目，只深入到足够完成本次修改的程度。

**核心原则**：不要试图理解整个项目，只聚焦与修改相关的区域。

**分层理解策略**：

```
第一层：宏观结构
  → 目录结构、入口文件、配置文件、依赖清单
  → 目的：知道项目是什么技术栈、大致分几个模块

第二层：数据流
  → 数据入口（API路由/页面组件）
  → 数据出口（数据库/外部API）
  → 数据流转路径
  → 目的：知道数据怎么在系统中流动

第三层：目标区域深入
  → 只深入理解与本次修改相关的代码区域
  → 阅读相关的函数/类/模块
  → 理解调用关系和依赖
  → 目的：知道怎么改、改哪里、改了会影响什么
```

**代码阅读时的记录**：在中大型修改时，可以将理解结果记录到 `docs/project-overview.md`（如果项目完全无文档则创建），为后续修改和未来维护提供参考。小型修改不强制补写文档。

> 📖 详细的代码理解策略和技巧，参见 `references/code-reading-strategy.md`

---

### M2 — 修改点分析 + 规模判断

**目标**：明确要改什么、影响范围多大、属于什么规模的修改。

**执行步骤**：

1. **精确定义修改点**：根据用户需求，明确需要修改/新增/删除的具体内容
2. **影响范围分析**：识别受影响的模块、文件、接口、数据结构
3. **规模判断**：根据影响范围判断修改规模

**规模判断标准**：

| 规模 | 判断依据 | 典型场景 |
|------|---------|---------|
| **小型** | 改动 ≤3 个文件，不影响接口和数据结构 | Bug修复、配置调整、文案修改、单文件改动 |
| **中型** | 改动 3-10 个文件，涉及接口或数据结构变更 | 新增/删除一个功能、修改接口、调整数据结构 |
| **大型** | 改动 >10 个文件，或涉及架构层面变更 | 新增模块、架构调整、多模块联动修改 |

**输出产物**：
- 修改点清单（要改什么）
- 影响范围（涉及哪些文件/模块/接口）
- 规模判定（小/中/大）
- 风险评估（可能出什么问题）

**根据规模进入不同流程**：

- **小型** → 直接修改 → 测试 → 提交
- **中型/大型** → 进入 M3 规划文档

---

### M3 — 规划文档（中型/大型修改）

**目标**：形成完整的修改规划，经用户确认后作为执行指引。

**核心原则**：规划文档是修改全程的指引。后续的 M4（执行修改）严格按规划文档进行，不允许偏离文档实现。如果执行中发现规划有问题，必须先修改规划文档再继续执行。

**规划文档必须包含的内容**：

1. **修改目标**：要达成什么效果
2. **修改范围**：涉及哪些文件/模块/接口
3. **修改步骤**：分步执行计划，每步的交付物和验证方式
4. **风险评估**：可能出的问题及应对方案
5. **回退方案**：如果修改失败，如何回退

**根据修改类型可能需要额外输出的文档**：

| 修改类型 | 额外文档 |
|---------|---------|
| 新增/修改接口 | 接口文档（新增接口的路径、参数、返回值） |
| 修改数据结构 | 数据模型变更文档（字段增删、类型变更） |
| 复杂修改 | 测试用例文档（覆盖正常/异常/边界场景） |
| 大型重构 | 架构变更文档（模块调整、依赖变化） |

**用户确认**：规划文档形成后，必须与用户确认。用户确认后方可进入 M4 执行。如果用户提出修改意见，调整规划后重新确认。

**Git 操作**：提交规划文档，commit message: `docs: add modification plan for <brief description>`

> 📖 规划文档的详细模板，参见 `references/document-templates.md` 中的规划文档部分

---

### M4 — 执行修改

**目标**：按规划文档实施修改，每步验证。

**核心原则**：代码与规划文档保持一致。任何偏离必须先更新规划文档再实现。

**小型修改执行流程**：

```
定位修改点 → 修改代码 → 添加注释 → 运行测试 → 完成
```

**中型/大型修改执行流程**：

```
按规划文档分步执行：
  对于每个步骤：
    1. 阅读规划文档中本步骤的要求
    2. 实现修改
    3. 添加必要的注释（新功能必须写清楚注释）
    4. 运行本步骤相关的测试
    5. 确认代码与规划文档一致
    6. 如果测试失败 → 进入回退流程
```

**代码风格要求**：
- 修改的代码应与项目已有的代码风格大体一致
- 新增的功能必须写清楚注释
- 修改已有代码时，如果原代码有注释，同步更新注释

**测试要求**：
- 修改前有测试 → 修改后运行已有测试确保不破坏
- 修改前无测试 → 根据项目复杂度和修改程度，提示用户确认是否需要补写测试
  - 简单项目/小改动 → 不强制补测试
  - 复杂项目/中大型改动 → 建议补写测试，用户确认

**关于偏离规划的处理**：
如果执行中发现规划文档有问题（如遗漏了影响、方案不可行），必须：
1. 暂停执行
2. 修改规划文档
3. 重新评估影响范围
4. 用户确认后继续执行

---

### M5 — 验证与收尾

**目标**：全面验证修改结果，同步更新文档，记录变更。

**执行步骤**：

1. **全量测试**：运行项目所有已有测试，确保没有破坏
2. **手动验证**：针对修改点进行手动验证（如果可行）
3. **文档更新**：
   - 同步更新与本次修改相关的文档
   - 发现的文档与代码不一致，记录但不主动全面修正（避免范围蔓延）
   - 无文档的项目，补写最小必要文档
4. **CHANGELOG 记录**：记录本次修改的内容
5. **Git 提交**：提交所有变更

**文档更新原则**：

| 文档状态 | 处理方式 |
|---------|---------|
| 有文档且准确 | 同步更新相关部分 |
| 有文档但过时 | 更新与本次修改相关的部分，其他不一致记录但不改 |
| 无文档 | 补写最小必要文档（项目概览 + 本次修改涉及的模块说明） |

**Git 提交**：
- 小型修改：一个 commit
- 中型/大型修改：执行过程中按步骤提交检查点（参见 `references/rollback-protocol.md` §1.2），M5 完成时提交最终变更
  - 如需整洁历史，可选择 squash 合并为一个 commit
  - commit message 使用 Conventional Commits 格式
- 示例：
  - `fix: resolve login redirect issue`
  - `feat: add user profile editing`
  - `refactor: extract authentication middleware`

---

## 三级回退策略

| 级别 | 触发条件 | 操作 | 升级条件 |
|------|---------|------|---------|
| Level 1 | 代码 bug，方案没问题 | 修代码 → 重测 | - |
| Level 2 | 方向偏差，需重来 | `git revert` 回退到本次修改前的 commit → 重新执行 | - |
| Level 3 | 需要回退到接手之前的记录 | **必须用户确认** → 回退 → 重新规划 | - |

### Level 3 特别说明

回退到接手之前的 Git 记录意味着可能丢失非本次修改的已有功能（如其他人的未提交修改）。因此：
- 执行 Level 3 前必须向用户明确说明回退的影响
- 用户确认后方可执行
- 回退后需要重新从 M1 开始理解项目状态

### 回退决策流程

```
修改/测试失败
     │
     ▼
 是代码bug吗？ ──YES──→ Level 1: 修复代码 → 重测
     │
     NO
     ▼
 方向偏差？ ──YES──→ Level 2: 回退到本次修改前 → 重新执行
     │
     NO
     ▼
 需要回退到接手前？ ──YES──→ 询问用户确认 → Level 3
     │
     NO
     ▼
 重新评估修改方案 → 更新规划文档 → 重新执行
```

> 📖 详细的回退操作步骤，参见 `references/rollback-protocol.md`

---

## 修改预览机制（复杂修改）

中大型修改中涉及接口变更、数据结构变更或架构调整的复杂场景，在实际编码前应展示修改计划供用户确认：

**预览内容**：
- 要修改/新增/删除的文件列表
- 每个文件的修改内容概述
- 预期影响范围
- 风险评估
- 回退方案

**确认流程**：
1. 展示修改预览
2. 用户确认 → 执行修改
3. 用户提出修改 → 调整规划文档 → 重新预览 → 再次确认

预览机制确保用户在修改发生前完全了解将要发生什么，避免修改后才发现方向错误。

---

## CHANGELOG 记录

每次修改完成后，在项目根目录的 `CHANGELOG.md` 中记录变更：

**格式**：
```markdown
## [YYYY-MM-DD] 修改类型：简要描述

### 变更内容
- 修改了 XXX（原因：...）
- 新增了 YYY（原因：...）
- 删除了 ZZZ（原因：...）

### 影响范围
- 涉及模块：...
- 涉及文件：...
- 接口变更：有/无

### 测试结果
- 已有测试：全部通过 / N 个失败
- 新增测试：N 个
```

如果 CHANGELOG.md 不存在，创建一个。新记录添加在文件顶部。

> 📖 CHANGELOG 的详细模板，参见 `references/document-templates.md`

---

## 文档管理策略

### 核心原则：代码为主，文档跟上

- 代码是运行的真实，文档只做参考
- 修改代码后，同步更新相关文档
- 不主动全面重写文档（避免范围蔓延）
- 每次修改后至少更新与本次修改相关的文档

### 文档与代码不一致的处理

| 发现时机 | 处理方式 |
|---------|---------|
| 代码理解阶段 | 记录不一致之处，作为参考，不影响当前修改 |
| 修改执行阶段 | 如果不一致影响本次修改，以代码为准执行，修改后同步更新文档 |
| 修改完成阶段 | 更新与本次修改相关的文档部分；其他不一致记录但不改 |

### 最小文档补写

对于完全没有文档的项目，在中大型修改时建议至少补写：
- `docs/project-overview.md`：项目概览（技术栈、目录结构、启动方式）

小型修改不强制补写文档。后续每次修改时，逐步补充与修改相关的模块/接口说明。不必一次补全所有文档。

---

## 关键场景处理

### 场景 1：修改时发现代码比预期复杂

1. 暂停修改
2. 重新深入理解代码（回到 M1 第三层）
3. 重新评估修改规模（可能需要升级为更高级别）
4. 如果升级为中型/大型，补写规划文档
5. 用户确认后继续

### 场景 2：修改引发了连锁问题

1. 评估连锁问题的影响范围
2. 如果可以快速修复 → Level 1 修复
3. 如果连锁问题较大 → Level 2 回退到本次修改前，重新规划
4. 更新规划文档，将连锁问题纳入考虑

### 场景 3：用户在修改过程中变更需求

1. 暂停当前修改
2. 评估新需求的影响
3. 更新规划文档（如果有）
4. 重新判断修改规模
5. 用户确认后继续

### 场景 4：项目没有测试且修改风险较高

1. 向用户说明风险
2. 建议补写测试
3. 用户确认是否补写测试
4. 根据用户决定继续

### 场景 5：修改涉及数据库结构变更

1. 在规划文档中明确数据库变更内容
2. 制定数据迁移方案（如何从旧结构迁移到新结构）
3. 制定回滚方案（如何从新结构回退到旧结构）
4. 用户确认后执行

---

## Quick Reference

| 文件 | 内容 | 何时阅读 |
|------|------|---------|
| `references/code-reading-strategy.md` | 代码理解策略和技巧 | M1 代码理解阶段 |
| `references/rollback-protocol.md` | 回退操作详情 | 需要执行回退时 |
| `references/document-templates.md` | CHANGELOG、规划文档、接口文档、数据模型、项目概览等模板 | M3 编写规划文档时 / M5 记录变更时 |

---

## Remember

- 代码是真相源，文档只做参考
- 本 skill 适用于对已有代码库的任何修改；项目尚不存在或需要从零搭建时不适用
- 当目标为已有项目时，此 skill 优先于项目构建类 skill
- 不要试图理解整个项目，只聚焦修改相关区域
- 中大型修改必须先形成规划文档再动手
- 规划文档是执行指引，不允许偏离文档实现
- 修改前确保 Git 安全网（当前状态已 commit）
- 回退到接手前的 Git 记录必须用户确认
- 修改后同步更新相关文档
- 新增功能必须写清楚注释
- 代码风格与项目大体一致即可
- 每次修改记录 CHANGELOG
- 小型修改轻量处理，不要过度流程化
- 测试补写由用户确认，不强制
- 中大型修改无文档时补写最小必要文档，小型修改不强制
